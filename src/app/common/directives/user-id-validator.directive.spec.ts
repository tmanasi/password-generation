import { UserIdValidatorDirective } from './user-id-validator.directive';

describe('UserIdValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UserIdValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
